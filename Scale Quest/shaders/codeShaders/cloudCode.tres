[gd_resource type="Shader" format=2]

[resource]
code = "shader_type canvas_item;

uniform vec2 direction;
uniform float velocity;

void fragment(){
	COLOR = texture(TEXTURE , UV + direction * velocity * TIME);
}"
